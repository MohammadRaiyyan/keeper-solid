export const paths = {
  HOME: "/",
  LOGIN: "/login",
  REGISTER: "/register",
  RESET_PASSWORD: "/reset-password",
};

export const publicPaths = [paths.LOGIN, paths.REGISTER, paths.RESET_PASSWORD];
